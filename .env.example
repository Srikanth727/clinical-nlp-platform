# ── Ollama (local AI — no API key needed) ────────────────────────────────────
# Model to use. Run: ollama pull llama3.2
OLLAMA_MODEL=llama3.2

# ── Optional (defaults shown) ─────────────────────────────────────────────────
# MinIO credentials
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio123
